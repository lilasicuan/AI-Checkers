/**
 * Will hold the following main methods:
 * - minimax(state) :
 * - player(state) : boolean
 * - terminalTest(state) : boolean
 */

 public class Minimax {
     public static void minimax(State s) {

     }    
 }